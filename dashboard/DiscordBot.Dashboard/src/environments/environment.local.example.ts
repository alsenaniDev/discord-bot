// Copy to environment.local.ts (gitignored) to override apiUrl locally without editing committed files.
export const environment = {
  production: false,
  apiUrl: 'https://discord-bot-production-b872.up.railway.app'
};
